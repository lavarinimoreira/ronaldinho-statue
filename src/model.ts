export interface TSphere {
    radius: string,
    material: string,
    units: string,
    weight: number,
    total: number,
    id: string
}

export interface TCylinder {
    radius: string,
    heigth: string,
    material: string,
    units: string,
    weight: number,
    total: number,
    id: string
}

export interface TParallelepiped {
    heigth: string,
    width: string,
    depth: string,
    material: string,
    units: string,
    weight: number,
    total: number,
    id: string
}