export interface TSphere {
    radius: string,
    material: string,
    units: string,
    weight: number,
    total: number,
    id: string
}

export interface TCylinder {
    radius: string,
    height: string,
    material: string,
    units: string,
    weight: number,
    total: number,
    id: string
}

export interface TParallelepiped {
    height: string,
    width: string,
    depth: string,
    material: string,
    units: string,
    weight: number,
    total: number,
    id: string
}