export interface TSphere {
    radius: string,
    material: string,
    units: string,
}

export interface TCylinder {
    radius: string,
    heigth: string,
    material: string,
    units: string,
}

export interface TParallelepiped {
    heigth: string,
    width: string,
    depth: string,
    material: string,
    units: string,
}